# frozen_string_literal: true

RSpec.describe Advantage::SalesforceSync do
  it "has a version number" do
    expect(Advantage::SalesforceSync::VERSION).not_to be nil
  end

  xit "does something useful" do
    expect(false).to eq(true)
  end
end
